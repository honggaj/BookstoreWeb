/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GenreResponseResultCustomModel } from '../../models/genre-response-result-custom-model';

export interface ApiGenreIdGet$Plain$Params {
  id: number;
}

export function apiGenreIdGet$Plain(http: HttpClient, rootUrl: string, params: ApiGenreIdGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<GenreResponseResultCustomModel>> {
  const rb = new RequestBuilder(rootUrl, apiGenreIdGet$Plain.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GenreResponseResultCustomModel>;
    })
  );
}

apiGenreIdGet$Plain.PATH = '/api/Genre/{id}';
