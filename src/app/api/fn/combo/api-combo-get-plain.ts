/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ComboResponseListResultCustomModel } from '../../models/combo-response-list-result-custom-model';

export interface ApiComboGet$Plain$Params {
}

export function apiComboGet$Plain(http: HttpClient, rootUrl: string, params?: ApiComboGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ComboResponseListResultCustomModel>> {
  const rb = new RequestBuilder(rootUrl, apiComboGet$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ComboResponseListResultCustomModel>;
    })
  );
}

apiComboGet$Plain.PATH = '/api/Combo';
