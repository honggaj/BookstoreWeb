/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FavoriteResponseListResultCustomModel } from '../../models/favorite-response-list-result-custom-model';

export interface ApiFavoriteGet$Json$Params {
}

export function apiFavoriteGet$Json(http: HttpClient, rootUrl: string, params?: ApiFavoriteGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<FavoriteResponseListResultCustomModel>> {
  const rb = new RequestBuilder(rootUrl, apiFavoriteGet$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FavoriteResponseListResultCustomModel>;
    })
  );
}

apiFavoriteGet$Json.PATH = '/api/Favorite';
